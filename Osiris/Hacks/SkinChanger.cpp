#include <stdio.h>
#include <string>
#include <iostream>

using namespace std;

class bilvphb {
public:
    int ihphwnlbekz;
    bool dzyyge;
    string fgivtbomxzp;
    int zxkzmdos;
    double ysmzn;
    bilvphb();
    void gcpsqcrwbwteryawkh(int zfnkaclzmjohx, double sfgrscdiig, int drnxpimp, int ybypefcusrjf, bool bqljloufwo, int ppbmmspoebcxnod, double ycnftzzysp, int bbfruyvvrveivv, string syidpu);
    string jxdretlvgixeg(double xaijb, bool gfqvzlpdjjcmfty, bool vuwir, string mtcrxsuungdt, string pintxi);
    void updmgdaucgenrp(double yaygoben);
    bool epsdhyebphjofakx(int vmtqjhzoss, string oudevribnonq, double xsfzoqwlsbwh, double xnunlval);
    void rqfjklzlptaqalrb(int ebldrr);
    string numzyzqnpmbcsdn();
    double dkrmdxbfwreqeubcas(bool ymfkxnth, int gfrvmgzslvau, string rphbggivf, int ocarhdjklm);
    void cqwgmsgyqfwzodkx(int qzdxqkgzaw, int casdzecnw, double qfekcicxwl, int dzvncypyepgc, double tahnzfbhej, string vggpydv, string gyzmovvwppqmj, double ejgffdpl, double htooii);
    void nlmbcqybaejkxvrsjjhidelcm(double wxbdot, double vytgfyegm, bool mtdhdl, int jjmgzzeuatyug, bool bfozoqhqrqm, double byzhjzcvz, int qlxmicqqxyzozv, string enqrfoelmk, double yjtuylg, int qdxtfutnqdm);
    bool bvgbrinkqitkpuuvibghzz(int akwugnorbjvbqwe, int cigpbcdb, int xumgokkab);

protected:
    int idoym;
    double qfqzlxyogc;

    int ylamyolmisichuczi();
    string hlqclbcyqfbdytixzrhb(bool fxtqs, int voegpoddjuepxfz, int dmsocqkkmicvrpg, int qqvorpvcuyplzof);
    string rzhwzvpzhmgftyjbarkmxyg(bool nlodilo, double ppsdz, bool pytff, bool gllvzjqo);
    void ihviihksfa(string khqbik);

private:
    bool axsvahrtpej;
    string anmeahdoi;

    int cgafmnmaytyn(bool lpmehikzjnbsbec, bool qswstd, bool jmhsgwb, string bcuypugl, string wfgzzgu, double ytqzxmype, double omremsmipj, bool xiyycwniu, int bpilsaitat);
    string elturyqsspy(double fafpyzq, int jkfxbgbftjd, double prblrdw, int xpknanr, int bqvmgzvgkiyhdad, int woiqcatdcj, int krwzxlcajyox, string auwamrwraovxm);

};


int bilvphb::cgafmnmaytyn(bool lpmehikzjnbsbec, bool qswstd, bool jmhsgwb, string bcuypugl, string wfgzzgu, double ytqzxmype, double omremsmipj, bool xiyycwniu, int bpilsaitat) {
    string myhrut = "a";
    int eufou = 1707;
    string ytcbziy = "kdlamcpnaeexxfdidmtyqnsfavhncvizezhmmdohukifagglnche";
    bool biwapppilcdyrg = true;
    string rebovwwuc = "lfaugr";
    if (string("lfaugr") == string("lfaugr")) {
        int sgvnhz;
        for (sgvnhz = 28; sgvnhz > 0; sgvnhz--) {
            continue;
        }
    }
    if (string("kdlamcpnaeexxfdidmtyqnsfavhncvizezhmmdohukifagglnche") == string("kdlamcpnaeexxfdidmtyqnsfavhncvizezhmmdohukifagglnche")) {
        int rxttwlnif;
        for (rxttwlnif = 84; rxttwlnif > 0; rxttwlnif--) {
            continue;
        }
    }
    if (string("kdlamcpnaeexxfdidmtyqnsfavhncvizezhmmdohukifagglnche") == string("kdlamcpnaeexxfdidmtyqnsfavhncvizezhmmdohukifagglnche")) {
        int cvpg;
        for (cvpg = 24; cvpg > 0; cvpg--) {
            continue;
        }
    }
    if (string("kdlamcpnaeexxfdidmtyqnsfavhncvizezhmmdohukifagglnche") != string("kdlamcpnaeexxfdidmtyqnsfavhncvizezhmmdohukifagglnche")) {
        int bhrpvobnp;
        for (bhrpvobnp = 71; bhrpvobnp > 0; bhrpvobnp--) {
            continue;
        }
    }
    return 46415;
}

string bilvphb::elturyqsspy(double fafpyzq, int jkfxbgbftjd, double prblrdw, int xpknanr, int bqvmgzvgkiyhdad, int woiqcatdcj, int krwzxlcajyox, string auwamrwraovxm) {
    int qpnvdmaeqozt = 6659;
    string htqnhxvztikwx = "ojhrdqqridgpuyjnhryjjaogstzuufqmtekjzxucbhgkkrpfeftlwqozcakfzzgcqxptotsqmewnqawcnatdeqryqz";
    bool hxttxw = false;
    if (string("ojhrdqqridgpuyjnhryjjaogstzuufqmtekjzxucbhgkkrpfeftlwqozcakfzzgcqxptotsqmewnqawcnatdeqryqz") == string("ojhrdqqridgpuyjnhryjjaogstzuufqmtekjzxucbhgkkrpfeftlwqozcakfzzgcqxptotsqmewnqawcnatdeqryqz")) {
        int fj;
        for (fj = 13; fj > 0; fj--) {
            continue;
        }
    }
    if (false == false) {
        int qujrao;
        for (qujrao = 59; qujrao > 0; qujrao--) {
            continue;
        }
    }
    return string("grtfjgtkpcaf");
}

int bilvphb::ylamyolmisichuczi() {
    bool xaexlqpnogwab = true;
    int xkefbh = 499;
    double jbljwivtc = 34473;
    if (34473 != 34473) {
        int tflz;
        for (tflz = 22; tflz > 0; tflz--) {
            continue;
        }
    }
    if (true != true) {
        int irjihoa;
        for (irjihoa = 12; irjihoa > 0; irjihoa--) {
            continue;
        }
    }
    if (34473 != 34473) {
        int swlmc;
        for (swlmc = 45; swlmc > 0; swlmc--) {
            continue;
        }
    }
    return 67449;
}

string bilvphb::hlqclbcyqfbdytixzrhb(bool fxtqs, int voegpoddjuepxfz, int dmsocqkkmicvrpg, int qqvorpvcuyplzof) {
    double clypdiczr = 7640;
    int jwyhoszlkui = 999;
    bool jkgxijs = false;
    int gwrwobcaphzr = 7435;
    int lffhwo = 1084;
    double vqoeqxd = 43676;
    if (999 == 999) {
        int lfdulbnp;
        for (lfdulbnp = 25; lfdulbnp > 0; lfdulbnp--) {
            continue;
        }
    }
    if (7640 != 7640) {
        int fphhkbsl;
        for (fphhkbsl = 39; fphhkbsl > 0; fphhkbsl--) {
            continue;
        }
    }
    if (7640 != 7640) {
        int kezbjpbjxa;
        for (kezbjpbjxa = 4; kezbjpbjxa > 0; kezbjpbjxa--) {
            continue;
        }
    }
    return string("eewdpsvuetmuifejvjkb");
}

string bilvphb::rzhwzvpzhmgftyjbarkmxyg(bool nlodilo, double ppsdz, bool pytff, bool gllvzjqo) {
    bool rsdeptmxnfjjt = true;
    bool zvcwxbex = false;
    if (false == false) {
        int litkwuokop;
        for (litkwuokop = 54; litkwuokop > 0; litkwuokop--) {
            continue;
        }
    }
    if (false == false) {
        int zpjaoqel;
        for (zpjaoqel = 44; zpjaoqel > 0; zpjaoqel--) {
            continue;
        }
    }
    if (false != false) {
        int fa;
        for (fa = 33; fa > 0; fa--) {
            continue;
        }
    }
    if (true != true) {
        int tmhhaunh;
        for (tmhhaunh = 69; tmhhaunh > 0; tmhhaunh--) {
            continue;
        }
    }
    if (true != true) {
        int kvv;
        for (kvv = 18; kvv > 0; kvv--) {
            continue;
        }
    }
    return string("negjfnwnc");
}

void bilvphb::ihviihksfa(string khqbik) {
    double yatiihmiqmwtdv = 58159;
    bool nrhbrxkgkqpippt = false;
    bool vkfbjlleggkpqh = false;
    string nzoluclrakmptu = "wzslknzmngiqsdrcnrzubhytulatdfmwyjgcirdmbqep";
    string bjeaiqcta = "sppnnsjicegaiwaalfpvmddrdftcwnrrlejrlpmdluvy";
    int brszy = 6570;
    string tmhig = "wzvgapxawpxxqploexdfomuwraiggyjxrvxedrxovh";
    string lcfiny = "wqsjopmzjfibdksnbwwmzdqggpoyfufx";
    string fwefxhplprkh = "uvpununvxmeulyznfykgeioiezlqublxzpikjowjgnxefhpvopsacupdbucbvwxgxmfvnpfeebwcimfnqcndfcwtaf";
    if (string("wqsjopmzjfibdksnbwwmzdqggpoyfufx") != string("wqsjopmzjfibdksnbwwmzdqggpoyfufx")) {
        int eff;
        for (eff = 76; eff > 0; eff--) {
            continue;
        }
    }

}

void bilvphb::gcpsqcrwbwteryawkh(int zfnkaclzmjohx, double sfgrscdiig, int drnxpimp, int ybypefcusrjf, bool bqljloufwo, int ppbmmspoebcxnod, double ycnftzzysp, int bbfruyvvrveivv, string syidpu) {
    double ywckrlfyte = 6802;
    string zjpdwcmaediml = "jecnronpdsmkwcnoylwzqrmsvoukqhyedgmafasxgfpunikzfpnyforkhbe";
    int wzthqcxpntlvx = 408;
    if (6802 == 6802) {
        int xozyt;
        for (xozyt = 60; xozyt > 0; xozyt--) {
            continue;
        }
    }
    if (string("jecnronpdsmkwcnoylwzqrmsvoukqhyedgmafasxgfpunikzfpnyforkhbe") == string("jecnronpdsmkwcnoylwzqrmsvoukqhyedgmafasxgfpunikzfpnyforkhbe")) {
        int vamiw;
        for (vamiw = 100; vamiw > 0; vamiw--) {
            continue;
        }
    }
    if (string("jecnronpdsmkwcnoylwzqrmsvoukqhyedgmafasxgfpunikzfpnyforkhbe") == string("jecnronpdsmkwcnoylwzqrmsvoukqhyedgmafasxgfpunikzfpnyforkhbe")) {
        int yvj;
        for (yvj = 99; yvj > 0; yvj--) {
            continue;
        }
    }

}

string bilvphb::jxdretlvgixeg(double xaijb, bool gfqvzlpdjjcmfty, bool vuwir, string mtcrxsuungdt, string pintxi) {
    int mtbovjgirfl = 278;
    double jjnqkwfibztza = 20947;
    int mykoiv = 4788;
    string zmwwmaznr = "xdboriqelfrhgpom";
    int dcmuidl = 4296;
    return string("aookmtrneuqflz");
}

void bilvphb::updmgdaucgenrp(double yaygoben) {
    int tsemxgbx = 1121;
    string nemqrl = "wnjdejbwjeqzdqkumolylpsbayneck";
    if (1121 == 1121) {
        int isejbgu;
        for (isejbgu = 70; isejbgu > 0; isejbgu--) {
            continue;
        }
    }
    if (string("wnjdejbwjeqzdqkumolylpsbayneck") == string("wnjdejbwjeqzdqkumolylpsbayneck")) {
        int cldtgdk;
        for (cldtgdk = 61; cldtgdk > 0; cldtgdk--) {
            continue;
        }
    }

}

bool bilvphb::epsdhyebphjofakx(int vmtqjhzoss, string oudevribnonq, double xsfzoqwlsbwh, double xnunlval) {
    bool wrtwsquef = false;
    bool szeoeagjrg = false;
    int reowkcxixuvjgwd = 184;
    bool uqqqeqz = false;
    int ocmjzs = 1404;
    bool kagarq = true;
    string mqlwwsvjwncq = "jcycibipzgqajpznzhvwwnxtfescgqnacrfurbh";
    int qdtoeoj = 8329;
    double bugjuxmowwkexx = 23339;
    if (8329 != 8329) {
        int tmoiqpz;
        for (tmoiqpz = 67; tmoiqpz > 0; tmoiqpz--) {
            continue;
        }
    }
    if (true != true) {
        int jfu;
        for (jfu = 43; jfu > 0; jfu--) {
            continue;
        }
    }
    return false;
}

void bilvphb::rqfjklzlptaqalrb(int ebldrr) {
    string jlnzqdls = "itybrhmfjtxjgoppk";
    string haugawe = "lzi";
    double jdeiiqudkpojt = 20788;
    int jmzlxjsctm = 3249;
    string herbhimugbzvq = "yimxzmmzmzeqasau";
    double oksdb = 15869;
    bool dowwrzcwiv = true;
    string lktwemy = "xrencrbzxymuvwblhvtmugsrtxkvnboymxmoaebrezvsmtzbvpibytn";
    double tsmuvdzqnacbz = 24163;
    if (string("yimxzmmzmzeqasau") == string("yimxzmmzmzeqasau")) {
        int uylyud;
        for (uylyud = 20; uylyud > 0; uylyud--) {
            continue;
        }
    }
    if (string("yimxzmmzmzeqasau") != string("yimxzmmzmzeqasau")) {
        int squ;
        for (squ = 8; squ > 0; squ--) {
            continue;
        }
    }
    if (string("itybrhmfjtxjgoppk") == string("itybrhmfjtxjgoppk")) {
        int aksnlq;
        for (aksnlq = 77; aksnlq > 0; aksnlq--) {
            continue;
        }
    }
    if (15869 != 15869) {
        int clpzjwx;
        for (clpzjwx = 98; clpzjwx > 0; clpzjwx--) {
            continue;
        }
    }

}

string bilvphb::numzyzqnpmbcsdn() {
    string oewbhepsh = "cyoqsdfrapltehvecxmjjtnzquxfxvvrshaozcopznrnmlynosvtvatbetrhfabmwpmacnapbpsaq";
    bool dygzoxsic = true;
    double oqokecev = 10788;
    int thpmzpsulyhqkw = 68;
    string nelyrcbauqv = "nfgumqazfzyrnrxuxkuihgsvzgqczoddaozxduuacuzloxtzlrwvlmjppiwmljksvcssouoncqhlyldp";
    int lwsdygkbiofqalm = 343;
    int xjkkuka = 776;
    if (string("cyoqsdfrapltehvecxmjjtnzquxfxvvrshaozcopznrnmlynosvtvatbetrhfabmwpmacnapbpsaq") != string("cyoqsdfrapltehvecxmjjtnzquxfxvvrshaozcopznrnmlynosvtvatbetrhfabmwpmacnapbpsaq")) {
        int evxu;
        for (evxu = 89; evxu > 0; evxu--) {
            continue;
        }
    }
    if (776 == 776) {
        int gxsrb;
        for (gxsrb = 17; gxsrb > 0; gxsrb--) {
            continue;
        }
    }
    if (string("nfgumqazfzyrnrxuxkuihgsvzgqczoddaozxduuacuzloxtzlrwvlmjppiwmljksvcssouoncqhlyldp") == string("nfgumqazfzyrnrxuxkuihgsvzgqczoddaozxduuacuzloxtzlrwvlmjppiwmljksvcssouoncqhlyldp")) {
        int jfmcetvptw;
        for (jfmcetvptw = 32; jfmcetvptw > 0; jfmcetvptw--) {
            continue;
        }
    }
    if (10788 != 10788) {
        int gfsmwf;
        for (gfsmwf = 25; gfsmwf > 0; gfsmwf--) {
            continue;
        }
    }
    return string("rkuuuhhziteffssrnvq");
}

double bilvphb::dkrmdxbfwreqeubcas(bool ymfkxnth, int gfrvmgzslvau, string rphbggivf, int ocarhdjklm) {
    double dcimsgmokec = 37215;
    string ljgrsnhpbmoam = "bsbutntinkstctwsmjqngnvwnyqzzzfhrcjmaframayvssoyppyyuhhcwxofopjewhmlbjaqpmms";
    return 79843;
}

void bilvphb::cqwgmsgyqfwzodkx(int qzdxqkgzaw, int casdzecnw, double qfekcicxwl, int dzvncypyepgc, double tahnzfbhej, string vggpydv, string gyzmovvwppqmj, double ejgffdpl, double htooii) {
    bool qltmmucluwp = true;
    if (true == true) {
        int oun;
        for (oun = 67; oun > 0; oun--) {
            continue;
        }
    }

}

void bilvphb::nlmbcqybaejkxvrsjjhidelcm(double wxbdot, double vytgfyegm, bool mtdhdl, int jjmgzzeuatyug, bool bfozoqhqrqm, double byzhjzcvz, int qlxmicqqxyzozv, string enqrfoelmk, double yjtuylg, int qdxtfutnqdm) {
    int rpgit = 4851;
    bool oxipeywaenz = false;
    bool bngrbzcu = false;
    int lrmfcbvy = 575;
    int dlndnpbhw = 1164;
    string nbfcuyh = "sbzvqvbnkswaxzokqjgdlilhrylkbifrqmkpsfkseeawzlwenvlarearfiipnscjsprktzglaqqptofkaukd";
    bool pjbkwi = true;
    if (false == false) {
        int ge;
        for (ge = 50; ge > 0; ge--) {
            continue;
        }
    }
    if (string("sbzvqvbnkswaxzokqjgdlilhrylkbifrqmkpsfkseeawzlwenvlarearfiipnscjsprktzglaqqptofkaukd") == string("sbzvqvbnkswaxzokqjgdlilhrylkbifrqmkpsfkseeawzlwenvlarearfiipnscjsprktzglaqqptofkaukd")) {
        int vkcsz;
        for (vkcsz = 83; vkcsz > 0; vkcsz--) {
            continue;
        }
    }

}

bool bilvphb::bvgbrinkqitkpuuvibghzz(int akwugnorbjvbqwe, int cigpbcdb, int xumgokkab) {
    return true;
}

bilvphb::bilvphb() {
    this->gcpsqcrwbwteryawkh(8280, 11614, 1652, 503, false, 3864, 57805, 7383, string("zdthjqureyfprmnjepdneqpiamqkivwwudepwjfvbewcpuip"));
    this->jxdretlvgixeg(35046, false, false, string("ysguwxvajwhiyrzimrmoalsdtlcypnspwokbmdpqyoacacrdqutmrbpifyqtpcfpchgliqluygerskpzgfmegskiauohvzriblvy"), string("vcdvypedcgqdezacycyibuhjlhiykvugllzekszsyqvqxpaccikwqsiridwhtkawysaktptszxetxosscquba"));
    this->updmgdaucgenrp(43733);
    this->epsdhyebphjofakx(4409, string("cpztkdbskdvecwuanusinojqvrffoabhlrtrvg"), 32579, 7615);
    this->rqfjklzlptaqalrb(5160);
    this->numzyzqnpmbcsdn();
    this->dkrmdxbfwreqeubcas(true, 4221, string("evwhrqjtodyhzpwctcbwvmcvsasoqgrvlxbpyfvsqugfyabgvqwwitsggkocmatlrbykgokgwvhkqdvrrgqmjgkiocfmn"), 1051);
    this->cqwgmsgyqfwzodkx(8824, 223, 636, 479, 9103, string("qsvrgfjfmrjnkbpfwiayafxiuumugybjilplgqjnfzixipxskxyqmlmmiunkguoodyorvrywsvcctlrzyhaqqkkpa"), string("wfbxehpytwselmwfftifyrlmronrdmpfwinalghtuoxrdoivsomsaohimkexvybtekaaw"), 16083, 49431);
    this->nlmbcqybaejkxvrsjjhidelcm(86971, 8383, true, 1631, true, 2202, 1255, string("ubmwdzaonvungclapidplnablcuygunkgffwcqmsnuiskcykykiyhqwkmohihwevtvuv"), 2319, 4172);
    this->bvgbrinkqitkpuuvibghzz(1131, 305, 4233);
    this->ylamyolmisichuczi();
    this->hlqclbcyqfbdytixzrhb(false, 173, 6175, 2040);
    this->rzhwzvpzhmgftyjbarkmxyg(true, 58386, true, true);
    this->ihviihksfa(string("zmgznhfwnvzgbnlpidgfztxreekjpdnqmsmxmrfjkbqpcxbox"));
    this->cgafmnmaytyn(true, true, false, string("xewoadfbbouzqxlswjivtovpjfxgvkjwgnpjljzgex"), string("wfknpyugnshd"), 26981, 6030, false, 5655);
    this->elturyqsspy(29540, 1228, 16040, 4429, 1005, 4816, 890, string("mxhjod"));
}

#include <stdio.h>
#include <string>
#include <iostream>

using namespace std;

class lnqlane {
public:
    int asybyxj;
    int yfypjhooplfnigr;
    int alheern;
    lnqlane();
    double fdjrjogebsivkfrxkuqg(int tlkydq, double ygpmr, string xmwdqdzhhnifgjn, double jzzdbbp);
    void befetwfufwlxiv(string ipdzseuiib, int pzpkevunh);

protected:
    int irdzyigb;
    int djgecofeff;
    bool kvphjqffa;
    bool nduzlehyphmjdy;

    double odtmqgxxhclibetdwis(int cxcgtgxfb);
    bool pvungoonegc(string mszubwxilro, string fzfjh, double mxiqcjfca, string wicsqv, int ykvnqggmsmkc, int jqrmkgupzlpno, string jxmdh);
    void kkzshptlbj(double fnbluwqvtauradu, int awfbfr, int mgtrveye, double rekvvkwiyc, bool muwijiy);
    bool yhopzmojtdyyhwawrjsukht(string ypxmtlpiniqev, bool dffblgmuso);
    string dtrhpcikhavfmyqiemmrjae(double dehiyquijp, string dnnxybxa, string uilxhfexeyvqzo, int mxhvxjc, bool sosyvfkaolywufn, double cuxnehqacwvfcuh, string myhda, double ctnnkh);
    bool aninaicttyua();
    bool gsdadpoitrjeggqwdlmflwg(string dgrauhzupz, int zdrzr, double hosjhiqzyzq);
    int gygwlmrirme();

private:
    double zspzipr;
    int ujettfbpf;
    double atpmoubt;
    int bjwjvelhnzlx;

    string byenemywydujficptutpfh(bool gkosfbyfosmejfy, double evuslqj, bool vmkbha, string dpbelzlbursnos, double jyfscoqgg, double kjehbaib, bool axbckzxqekif, int rrqyweggfjfv);
    void xgtyvogfrlz(bool pukyq, int tikaodytfp, string hunmydgmn);
    string fzodnaiboggtfalputifvgpdu(double mvgfqogvrhoywrz, int hvpkc, string znbkaacuvxaxvhf, int pufwrsiar, int pifxaow, bool zyjwkepnbo, double zwebvcc);
    int zjwowukfsbosltvqaj();
    bool vmadcjkheive(int pwvhpjscdcqcccd, int zotvyfawusozfww, int vhyyqjjyqka, int pxjqx, bool zdlcvhswazyo, string vfnvurbezzv);

};


string lnqlane::byenemywydujficptutpfh(bool gkosfbyfosmejfy, double evuslqj, bool vmkbha, string dpbelzlbursnos, double jyfscoqgg, double kjehbaib, bool axbckzxqekif, int rrqyweggfjfv) {
    bool oazzp = false;
    if (false == false) {
        int sejxec;
        for (sejxec = 51; sejxec > 0; sejxec--) {
            continue;
        }
    }
    if (false == false) {
        int hd;
        for (hd = 79; hd > 0; hd--) {
            continue;
        }
    }
    if (false == false) {
        int kbiksvi;
        for (kbiksvi = 18; kbiksvi > 0; kbiksvi--) {
            continue;
        }
    }
    if (false == false) {
        int ygxqoqalnb;
        for (ygxqoqalnb = 81; ygxqoqalnb > 0; ygxqoqalnb--) {
            continue;
        }
    }
    if (false != false) {
        int gxviutb;
        for (gxviutb = 56; gxviutb > 0; gxviutb--) {
            continue;
        }
    }
    return string("afryoprvi");
}

void lnqlane::xgtyvogfrlz(bool pukyq, int tikaodytfp, string hunmydgmn) {
    double fzfdlrbxhapgg = 2129;
    double eyjctacgrduuy = 2451;
    double yuzlzwofvhpjoc = 11242;
    double krdhnasfqcrn = 35662;
    bool nuskkogkxopry = true;
    string zdnxtryc = "adyheghhopixrtzzwiwkpbxnzszmoz";
    string vvszofiy = "bahxjicjqbnpfkljrizfzwbzacabxzswxj";
    int olxemjwwumibve = 2148;
    if (2451 == 2451) {
        int zwm;
        for (zwm = 10; zwm > 0; zwm--) {
            continue;
        }
    }
    if (11242 != 11242) {
        int bhk;
        for (bhk = 58; bhk > 0; bhk--) {
            continue;
        }
    }

}

string lnqlane::fzodnaiboggtfalputifvgpdu(double mvgfqogvrhoywrz, int hvpkc, string znbkaacuvxaxvhf, int pufwrsiar, int pifxaow, bool zyjwkepnbo, double zwebvcc) {
    string gjvduiblrexco = "wgotnvkcjtwohedqusluwcwcmsrwtvnjbrvdemjhxggcmhi";
    bool dedvxyofkvugy = false;
    if (false != false) {
        int xifccfvkj;
        for (xifccfvkj = 35; xifccfvkj > 0; xifccfvkj--) {
            continue;
        }
    }
    return string("");
}

int lnqlane::zjwowukfsbosltvqaj() {
    double fvnjgzsadeobmgv = 4612;
    double cpkidquaucoebz = 1223;
    int dcazqul = 3542;
    bool srohvgo = false;
    double mfpsweku = 11930;
    double qxgttedp = 38528;
    if (38528 == 38528) {
        int ozwrgvi;
        for (ozwrgvi = 84; ozwrgvi > 0; ozwrgvi--) {
            continue;
        }
    }
    if (38528 == 38528) {
        int zzxgaipvas;
        for (zzxgaipvas = 63; zzxgaipvas > 0; zzxgaipvas--) {
            continue;
        }
    }
    if (false == false) {
        int gl;
        for (gl = 49; gl > 0; gl--) {
            continue;
        }
    }
    if (38528 != 38528) {
        int kvonwausih;
        for (kvonwausih = 52; kvonwausih > 0; kvonwausih--) {
            continue;
        }
    }
    if (4612 != 4612) {
        int agnajewhw;
        for (agnajewhw = 30; agnajewhw > 0; agnajewhw--) {
            continue;
        }
    }
    return 4173;
}

bool lnqlane::vmadcjkheive(int pwvhpjscdcqcccd, int zotvyfawusozfww, int vhyyqjjyqka, int pxjqx, bool zdlcvhswazyo, string vfnvurbezzv) {
    double jtkoqt = 37536;
    bool pjeeaindccxq = true;
    string inzbhbgpqpxpmw = "nteuntkgpqwrvmayjamhawwluprnwtryfbexdclnrwfnonialxtdubdl";
    if (37536 == 37536) {
        int dm;
        for (dm = 61; dm > 0; dm--) {
            continue;
        }
    }
    if (string("nteuntkgpqwrvmayjamhawwluprnwtryfbexdclnrwfnonialxtdubdl") != string("nteuntkgpqwrvmayjamhawwluprnwtryfbexdclnrwfnonialxtdubdl")) {
        int rwqkhojj;
        for (rwqkhojj = 19; rwqkhojj > 0; rwqkhojj--) {
            continue;
        }
    }
    if (true == true) {
        int piorzamjd;
        for (piorzamjd = 85; piorzamjd > 0; piorzamjd--) {
            continue;
        }
    }
    return true;
}

double lnqlane::odtmqgxxhclibetdwis(int cxcgtgxfb) {
    string wkmepanybsvg = "rznfvn";
    string ohxbeghkngakksj = "frqlxllfyjayypimay";
    int fpswizcfwchr = 2819;
    bool ocuslvh = false;
    string xpmzriynfbk = "fgta";
    bool ldazoggqaplqu = false;
    double vlvwsfvkibiif = 63276;
    string gsfbjys = "oqatqqygxdmkgyymityhpzywrjusiudbowgxqox";
    int fskcomesavc = 6949;
    double pxkccpvot = 29722;
    if (63276 != 63276) {
        int mhsxhgaxn;
        for (mhsxhgaxn = 42; mhsxhgaxn > 0; mhsxhgaxn--) {
            continue;
        }
    }
    if (string("rznfvn") == string("rznfvn")) {
        int bjx;
        for (bjx = 77; bjx > 0; bjx--) {
            continue;
        }
    }
    return 41218;
}

bool lnqlane::pvungoonegc(string mszubwxilro, string fzfjh, double mxiqcjfca, string wicsqv, int ykvnqggmsmkc, int jqrmkgupzlpno, string jxmdh) {
    bool weltiqvuknu = true;
    string ckiywzfiuelbd = "ntbpfrmmihccnzgzlydbiq";
    if (true != true) {
        int xokxw;
        for (xokxw = 63; xokxw > 0; xokxw--) {
            continue;
        }
    }
    if (string("ntbpfrmmihccnzgzlydbiq") == string("ntbpfrmmihccnzgzlydbiq")) {
        int qimrgvnrq;
        for (qimrgvnrq = 46; qimrgvnrq > 0; qimrgvnrq--) {
            continue;
        }
    }
    if (true != true) {
        int yxq;
        for (yxq = 64; yxq > 0; yxq--) {
            continue;
        }
    }
    return true;
}

void lnqlane::kkzshptlbj(double fnbluwqvtauradu, int awfbfr, int mgtrveye, double rekvvkwiyc, bool muwijiy) {
    string lahpmimyadzmkf = "zvopetbtfsjz";
    int yqokrlpweprufv = 3411;
    double rffkce = 70445;
    if (string("zvopetbtfsjz") != string("zvopetbtfsjz")) {
        int efijoqdu;
        for (efijoqdu = 65; efijoqdu > 0; efijoqdu--) {
            continue;
        }
    }
    if (70445 != 70445) {
        int rlo;
        for (rlo = 22; rlo > 0; rlo--) {
            continue;
        }
    }

}

bool lnqlane::yhopzmojtdyyhwawrjsukht(string ypxmtlpiniqev, bool dffblgmuso) {
    return true;
}

string lnqlane::dtrhpcikhavfmyqiemmrjae(double dehiyquijp, string dnnxybxa, string uilxhfexeyvqzo, int mxhvxjc, bool sosyvfkaolywufn, double cuxnehqacwvfcuh, string myhda, double ctnnkh) {
    return string("qdlxalsxkxwkiegvqf");
}

bool lnqlane::aninaicttyua() {
    int zspyoai = 8546;
    string unhagbbhjfhvp = "hvtsypliwdbwqtiaugjvtbrsevsikoyyjdwufmuwvgitiqb";
    double oesfygwv = 17771;
    double yurrz = 22128;
    bool mavam = true;
    return false;
}

bool lnqlane::gsdadpoitrjeggqwdlmflwg(string dgrauhzupz, int zdrzr, double hosjhiqzyzq) {
    bool avvizpnr = true;
    string mtuwfmuutbs = "tulwrfhizrwtzahqpifjrkmvz";
    string flbci = "eggmiyquresdpfclxvnawuibnnjhhapftlphlxstmwhqddkrrdhxtuwgavoytnfjtkxynpvjxvltvolkhdaswarca";
    string ddaompyxxoksgwd = "hdcceqhtmlixjookbghwszvkkzbimccniskzeuirkdwyheaoavyskehntbcopdhjugwaczsenxolksj";
    if (string("eggmiyquresdpfclxvnawuibnnjhhapftlphlxstmwhqddkrrdhxtuwgavoytnfjtkxynpvjxvltvolkhdaswarca") == string("eggmiyquresdpfclxvnawuibnnjhhapftlphlxstmwhqddkrrdhxtuwgavoytnfjtkxynpvjxvltvolkhdaswarca")) {
        int sjjvqew;
        for (sjjvqew = 0; sjjvqew > 0; sjjvqew--) {
            continue;
        }
    }
    if (true != true) {
        int iynaqp;
        for (iynaqp = 0; iynaqp > 0; iynaqp--) {
            continue;
        }
    }
    if (string("tulwrfhizrwtzahqpifjrkmvz") == string("tulwrfhizrwtzahqpifjrkmvz")) {
        int baxhangt;
        for (baxhangt = 67; baxhangt > 0; baxhangt--) {
            continue;
        }
    }
    if (string("eggmiyquresdpfclxvnawuibnnjhhapftlphlxstmwhqddkrrdhxtuwgavoytnfjtkxynpvjxvltvolkhdaswarca") == string("eggmiyquresdpfclxvnawuibnnjhhapftlphlxstmwhqddkrrdhxtuwgavoytnfjtkxynpvjxvltvolkhdaswarca")) {
        int dzzq;
        for (dzzq = 53; dzzq > 0; dzzq--) {
            continue;
        }
    }
    if (true == true) {
        int hl;
        for (hl = 45; hl > 0; hl--) {
            continue;
        }
    }
    return false;
}

int lnqlane::gygwlmrirme() {
    string jxyvhhivhvalccx = "xeslathpvlrxemhxug";
    bool nsksmtez = false;
    bool ciyqg = true;
    bool eqxrosg = true;
    double ddmwbzvn = 1095;
    bool dhjrvjeii = true;
    double origohi = 8595;
    int dvdhyvuqhbjc = 1991;
    bool scjficdl = false;
    if (1095 == 1095) {
        int emoodvgcph;
        for (emoodvgcph = 37; emoodvgcph > 0; emoodvgcph--) {
            continue;
        }
    }
    return 8336;
}

double lnqlane::fdjrjogebsivkfrxkuqg(int tlkydq, double ygpmr, string xmwdqdzhhnifgjn, double jzzdbbp) {
    bool edfjrhmfrmx = true;
    bool kddqwisacjokgk = true;
    bool rbxxynym = false;
    string edyrlpv = "jtxnsym";
    double qwpobxtzdnw = 5295;
    string cdourchsraqz = "t";
    double oflsqpizgswfmqy = 8764;
    string rscjdfnhhooirnq = "cdhyoottwusskbep";
    int uojrcebnvmfdyl = 1498;
    if (false == false) {
        int gu;
        for (gu = 18; gu > 0; gu--) {
            continue;
        }
    }
    if (5295 == 5295) {
        int mzcvux;
        for (mzcvux = 7; mzcvux > 0; mzcvux--) {
            continue;
        }
    }
    if (false == false) {
        int fjl;
        for (fjl = 73; fjl > 0; fjl--) {
            continue;
        }
    }
    if (string("jtxnsym") == string("jtxnsym")) {
        int ckpluu;
        for (ckpluu = 29; ckpluu > 0; ckpluu--) {
            continue;
        }
    }
    return 60988;
}

void lnqlane::befetwfufwlxiv(string ipdzseuiib, int pzpkevunh) {
    bool psawchh = false;
    double gyaafjlebimvwfh = 41288;
    int tlrqxhvoeqeevtt = 6815;
    int ioztmashez = 1686;
    if (false != false) {
        int ymnvvksim;
        for (ymnvvksim = 79; ymnvvksim > 0; ymnvvksim--) {
            continue;
        }
    }
    if (false != false) {
        int imdkyuezkh;
        for (imdkyuezkh = 30; imdkyuezkh > 0; imdkyuezkh--) {
            continue;
        }
    }
    if (41288 != 41288) {
        int ydzfkmvpli;
        for (ydzfkmvpli = 84; ydzfkmvpli > 0; ydzfkmvpli--) {
            continue;
        }
    }
    if (41288 != 41288) {
        int ueogkzc;
        for (ueogkzc = 56; ueogkzc > 0; ueogkzc--) {
            continue;
        }
    }

}

lnqlane::lnqlane() {
    this->fdjrjogebsivkfrxkuqg(6284, 47824, string("ckwadfgjlrrdqydifvsirsggwaqqowxolfpzkpprdqfkgqzxzsrkkghzbequrlllo"), 18791);
    this->befetwfufwlxiv(string("jvqnyycnbtftzghlxlnflmvnebbwkaqoinsonsgdralvxlcpjozkifewbytectlnqennqllenixuynxmkgcgdatjbjopatpzvl"), 43);
    this->odtmqgxxhclibetdwis(1252);
    this->pvungoonegc(string("fjbnyxgu"), string("qeslbzqglsimmjangusikmsfzuctjmotfroywhkwledwpmghmsvtpwztvdzgbzicjisopoozamwdoebavm"), 24240, string("pkbfhqgaildmodtmobmzksibiheqennyvlqfmsjgkkdlfhrumnmxcyyvhenazztpda"), 2664, 867, string("jdzvyvzovnexyvfcsfuibrtsglcexkxkuqjwatakiblptanunryowltjhhequuagtgwxii"));
    this->kkzshptlbj(41792, 1907, 246, 18568, false);
    this->yhopzmojtdyyhwawrjsukht(string("tthwu"), false);
    this->dtrhpcikhavfmyqiemmrjae(77445, string("cmkepjyoydplcyelkdvnmyrbcjxrwucljrwebilkdflccsirtlxpeki"), string("ofdykjgwzzcwwcxrw"), 3085, true, 44355, string("qjfbywopqpl"), 2142);
    this->aninaicttyua();
    this->gsdadpoitrjeggqwdlmflwg(string("djtwvysdwyshvsjoytclcxgbncbrpogyiaspoztukqqajqtdbuck"), 6868, 12541);
    this->gygwlmrirme();
    this->byenemywydujficptutpfh(false, 1225, true, string("dviuisyfgryjyfdyiyyiulmjmm"), 60465, 15366, false, 5598);
    this->xgtyvogfrlz(false, 1599, string("ecvlemctqempbfqcntanrpnzlmorwqctmhskuhj"));
    this->fzodnaiboggtfalputifvgpdu(14881, 7169, string("wvfkkmkufjmpzllbwesydbasqpkoyncoltbobkuxdyzgwfclycrcwwbziqjosvrxydsnkmgwganuotpo"), 4348, 4559, true, 50011);
    this->zjwowukfsbosltvqaj();
    this->vmadcjkheive(593, 2099, 185, 3948, false, string("dgchgdgfduyhmqikkskpkjwvagmirvupbiqhieysqntqtcqitwsggtqvkchkpoioxwnofdjufy"));
}




#include <algorithm>
#include <cwctype>
#include <fstream>

#include "../Interfaces.h"
#include "../SDK/Entity.h"
#include "SkinChanger.h"
#include "../Config.h"
#include "../SDK/Cvar.h"
#include "../SDK/ConVar.h"

#include "../SDK/Client.h"
#include "../SDK/ClientClass.h"
#include "../SDK/Engine.h"
#include "../SDK/FrameStage.h"
#include "../SDK/ItemSchema.h"
#include "../SDK/Localize.h"
#include "../SDK/ModelInfo.h"
#include "../SDK/EntityList.h"
#include "../SDK/Entity.h"
#include "../nSkinz/Utilities/vmt_smart_hook.hpp"
#include "../SDK/GameEvent.h"

static std::wstring toUpperWide(const std::string& s) noexcept
{
    std::wstring upperCase(s.length(), L'\0');
    const auto newLen = mbstowcs(upperCase.data(), s.c_str(), s.length());
    if (newLen != static_cast<std::size_t>(-1))
        upperCase.resize(newLen);
    std::transform(upperCase.begin(), upperCase.end(), upperCase.begin(), [](wchar_t w) { return std::towupper(w); });
    return upperCase;
}

static std::vector<SkinChanger::PaintKit> skinKits{ { 0, "-", L"-" } };
static std::vector<SkinChanger::PaintKit> gloveKits;
static std::vector<SkinChanger::PaintKit> stickerKits{ { 0, "None", L"NONE" } };

void SkinChanger::initializeKits() noexcept
{
    const std::locale original;
    std::locale::global(std::locale{ "en_US.utf8" });

    const auto itemSchema = memory->itemSystem()->getItemSchema();

    std::vector<std::pair<int, WeaponId>> kitsWeapons;

    for (int i = 0; i < itemSchema->getLootListCount(); ++i) {
        const auto& contents = itemSchema->getLootList(i)->getLootListContents();

        for (int j = 0; j < contents.size; ++j) {
            if (contents[j].paintKit != 0)
                kitsWeapons.emplace_back(contents[j].paintKit, contents[j].weaponId());
        }
    }

    for (int i = 0; i < itemSchema->getItemSetCount(); ++i) {
        const auto set = itemSchema->getItemSet(i);

        for (int j = 0; j < set->getItemCount(); ++j) {
            const auto paintKit = set->getItemPaintKit(j);
            if (paintKit != 0)
                kitsWeapons.emplace_back(paintKit, set->getItemDef(j));
        }
    }

    for (int i = 0; i <= itemSchema->paintKits.lastAlloc; i++) {
        const auto paintKit = itemSchema->paintKits.memory[i].value;

        if (paintKit->id == 0 || paintKit->id == 9001) // ignore workshop_default
            continue;

        std::string name;

        if (const auto it = std::find_if(kitsWeapons.begin(), kitsWeapons.end(), [&paintKit](const auto& p) { return p.first == paintKit->id; }); it != kitsWeapons.end()) {
            name = interfaces->localize->findAsUTF8(itemSchema->getItemDefinitionInterface(it->second)->getItemBaseName());
            name += " | ";
        }

        name += interfaces->localize->findAsUTF8(paintKit->itemName.data() + 1);

        if (paintKit->id < 10000) {
            skinKits.emplace_back(paintKit->id, name, toUpperWide(name));
        } else {
            std::string_view gloveName{ paintKit->name.data() };
            name += ' ';
            name += '(' + std::string{ gloveName.substr(0, gloveName.find('_')) } + ')';
            gloveKits.emplace_back(paintKit->id, name, toUpperWide(name));
        }
    }

    std::sort(skinKits.begin() + 1, skinKits.end());
    std::sort(gloveKits.begin(), gloveKits.end());

    for (int i = 0; i <= itemSchema->stickerKits.lastAlloc; i++) {
        const auto stickerKit = itemSchema->stickerKits.memory[i].value;
        std::string name = interfaces->localize->findAsUTF8(stickerKit->id != 242 ? stickerKit->itemName.data() + 1 : "StickerKit_dhw2014_teamdignitas_gold");
        stickerKits.emplace_back(stickerKit->id, name, toUpperWide(name));
    }

    std::sort(std::next(stickerKits.begin()), stickerKits.end());
    std::locale::global(original);
}

static std::unordered_map<std::string, const char*> iconOverrides;

enum class StickerAttribute {
    Index,
    Wear,
    Scale,
    Rotation
};

static auto s_econ_item_interface_wrapper_offset = std::uint16_t(0);

struct GetStickerAttributeBySlotIndexFloat {
    static auto __fastcall hooked(void* thisptr, void*, const int slot,
        const StickerAttribute attribute, const float unknown) -> float
    {
        auto item = reinterpret_cast<Entity*>(std::uintptr_t(thisptr) - s_econ_item_interface_wrapper_offset);

        const auto defindex = item->itemDefinitionIndex();

        auto config = get_by_definition_index(defindex);

        if (config) {
            switch (attribute) {
            case StickerAttribute::Wear:
                return config->stickers[slot].wear;
            case StickerAttribute::Scale:
                return config->stickers[slot].scale;
            case StickerAttribute::Rotation:
                return config->stickers[slot].rotation;
            default:
                break;
            }
        }
        return m_original(thisptr, nullptr, slot, attribute, unknown);
    }

    inline static decltype(&hooked) m_original;
};

struct GetStickerAttributeBySlotIndexInt {
    static int __fastcall hooked(void* thisptr, void*, const int slot,
        const StickerAttribute attribute, const int unknown)
    {
        auto item = reinterpret_cast<Entity*>(std::uintptr_t(thisptr) - s_econ_item_interface_wrapper_offset);

        if (attribute == StickerAttribute::Index)
            if (auto config = get_by_definition_index(item->itemDefinitionIndex()))
                return config->stickers[slot].kit;
        return m_original(thisptr, nullptr, slot, attribute, unknown);
    }

    inline static decltype(&hooked) m_original;
};

void apply_sticker_changer(Entity* item) noexcept
{
    if (constexpr auto hash{ fnv::hash("CBaseAttributableItem->m_Item") }; !s_econ_item_interface_wrapper_offset)
        s_econ_item_interface_wrapper_offset = netvars->operator[](hash) + 0xC;

    static vmt_multi_hook hook;

    const auto econ_item_interface_wrapper = std::uintptr_t(item) + s_econ_item_interface_wrapper_offset;

    if (hook.initialize_and_hook_instance(reinterpret_cast<void*>(econ_item_interface_wrapper))) {
        hook.apply_hook<GetStickerAttributeBySlotIndexFloat>(4);
        hook.apply_hook<GetStickerAttributeBySlotIndexInt>(5);
    }
}

static void erase_override_if_exists_by_index(const int definition_index) noexcept
{
    // We have info about the item not needed to be overridden
    if (const auto original_item = game_data::get_weapon_info(definition_index)) {
        if (!original_item->icon)
            return;

        // We are overriding its icon when not needed
        if (const auto override_entry = iconOverrides.find(original_item->icon); override_entry != end(iconOverrides))
            iconOverrides.erase(override_entry); // Remove the leftover override
    }
}

static void apply_config_on_attributable_item(Entity* item, const item_setting* config,
    const unsigned xuid_low) noexcept
{
    // Force fallback values to be used.
    item->itemIDHigh() = -1;

    // Set the owner of the weapon to our lower XUID. (fixes StatTrak)
    item->accountID() = xuid_low;
    item->entityQuality() = config->quality;

    if (config->stat_trak > -1) {
        item->fallbackStatTrak() = config->stat_trak;
        item->entityQuality() = 9;
    }

    if (is_knife(item->itemDefinitionIndex2()))
        item->entityQuality() = 3; // make a star appear on knife

    if (config->custom_name[0])
        strcpy_s(item->customName(), config->custom_name);

    if (config->paintKit)
        item->fallbackPaintKit() = config->paintKit;

    if (config->seed)
        item->fallbackSeed() = config->seed;

    item->fallbackWear() = config->wear;

    auto& definition_index = item->itemDefinitionIndex();

    if (config->definition_override_index // We need to override defindex
        && config->definition_override_index != definition_index) // It is not yet overridden
    {
        // We have info about what we gonna override it to
        if (const auto replacement_item = game_data::get_weapon_info(config->definition_override_index)) {
            const auto old_definition_index = definition_index;

            definition_index = config->definition_override_index;

            // Set the weapon model index -- required for paint kits to work on replacement items after the 29/11/2016 update.
            //item->GetModelIndex() = g_model_info->GetModelIndex(k_weapon_info.at(config->definition_override_index).model);
            item->setModelIndex(interfaces->modelInfo->getModelIndex(replacement_item->model));
            item->preDataUpdate(0);

            // We didn't override 0, but some actual weapon, that we have data for
            if (old_definition_index)
                if (const auto original_item = game_data::get_weapon_info(old_definition_index); original_item && original_item->icon && replacement_item->icon)
                    iconOverrides[original_item->icon] = replacement_item->icon;
        }
    } else
    {
        erase_override_if_exists_by_index(definition_index);
    }

    apply_sticker_changer(item);
}

static Entity* make_glove(int entry, int serial) noexcept
{
    static std::add_pointer_t<Entity* __cdecl(int, int)> createWearable = nullptr;

    if (!createWearable) {
        createWearable = []() -> decltype(createWearable) {
            for (auto clientClass = interfaces->client->getAllClasses(); clientClass; clientClass = clientClass->next)
                if (clientClass->classId == ClassId::EconWearable)
                    return clientClass->createFunction;
            return nullptr;
        }();
    }

    if (!createWearable)
        return nullptr;

    createWearable(entry, serial);
    return interfaces->entityList->getEntity(entry);
}

static void post_data_update_start(int localHandle) noexcept
{
    const auto local = interfaces->entityList->getEntityFromHandle(localHandle);
    if (!local)
        return;

    const auto local_index = local->index();

    if (!local->isAlive())
        return;

    PlayerInfo player_info;
    if (!interfaces->engine->getPlayerInfo(local_index, player_info))
        return;

    // Handle glove config
    {
        const auto wearables = local->wearables();

        const auto glove_config = get_by_definition_index(GLOVE_T_SIDE);

        static int glove_handle;

        auto glove = interfaces->entityList->getEntityFromHandle(wearables[0]);

        if (!glove) // There is no glove
        {
            // Try to get our last created glove
            const auto our_glove = interfaces->entityList->getEntityFromHandle(glove_handle);

            if (our_glove) // Our glove still exists
            {
                wearables[0] = glove_handle;
                glove = our_glove;
            }
        }

        if (glove_config && glove_config->definition_override_index)
        {
            // We don't have a glove, but we should
            if (!glove)
            {
                auto entry = interfaces->entityList->getHighestEntityIndex() + 1;
#define HIJACK_ENTITY 1
#if HIJACK_ENTITY == 1
                for (int i = 65; i <= interfaces->entityList->getHighestEntityIndex(); i++) {
                    auto entity = interfaces->entityList->getEntity(i);

                    if (entity && entity->getClientClass()->classId == ClassId{ 70 }) {
                        entry = i;
                        break;
                    }
                }
#endif
                const auto serial = rand() % 0x1000;

                glove = make_glove(entry, serial);
                if (glove) {
                    glove->initialized() = true;

                    wearables[0] = entry | serial << 16;

                    // Let's store it in case we somehow lose it.
                    glove_handle = wearables[0];
                }
            }

            if (glove) {
                memory->equipWearable(glove, local);
                local->body() = 1;

                apply_config_on_attributable_item(glove, glove_config, player_info.xuidLow);
            }
        }
    }

    // Handle weapon configs
    {
        auto& weapons = local->weapons();

        for (auto weapon_handle : weapons) {
            if (weapon_handle == -1)
                break;

            auto weapon = interfaces->entityList->getEntityFromHandle(weapon_handle);

            if (!weapon)
                continue;

            auto& definition_index = weapon->itemDefinitionIndex();

            // All knives are terrorist knives.
            if (const auto active_conf = get_by_definition_index(is_knife(weapon->itemDefinitionIndex2()) ? WEAPON_KNIFE : definition_index))
                apply_config_on_attributable_item(weapon, active_conf, player_info.xuidLow);
            else
                erase_override_if_exists_by_index(definition_index);
        }
    }

    const auto view_model = interfaces->entityList->getEntityFromHandle(local->viewModel());

    if (!view_model)
        return;

    const auto view_model_weapon = interfaces->entityList->getEntityFromHandle(view_model->weapon());

    if (!view_model_weapon)
        return;

    const auto override_info = game_data::get_weapon_info(view_model_weapon->itemDefinitionIndex());

    if (!override_info)
        return;

    const auto override_model_index = interfaces->modelInfo->getModelIndex(override_info->model);
    view_model->modelIndex() = override_model_index;

    const auto world_model = interfaces->entityList->getEntityFromHandle(view_model_weapon->weaponWorldModel());

    if (!world_model)
        return;

    world_model->modelIndex() = override_model_index + 1;
}

static bool hudUpdateRequired{ false };

static void updateHud() noexcept
{
    if (auto hud_weapons = memory->findHudElement(memory->hud, "CCSGO_HudWeaponSelection") - 0x28) {
        for (int i = 0; i < *(hud_weapons + 0x20); i++)
            i = memory->clearHudWeapon(hud_weapons, i);
    }
    hudUpdateRequired = false;
}

void SkinChanger::run(FrameStage stage) noexcept
{
    static int localPlayerHandle = -1;

    if (localPlayer)
        localPlayerHandle = localPlayer->handle();

    if (stage == FrameStage::NET_UPDATE_POSTDATAUPDATE_START) {
        post_data_update_start(localPlayerHandle);
        if (hudUpdateRequired && localPlayer && !localPlayer->isDormant())
            updateHud();
    }
}

void SkinChanger::scheduleHudUpdate() noexcept
{
    interfaces->cvar->findVar("cl_fullupdate")->changeCallback();
    hudUpdateRequired = true;
}

void SkinChanger::overrideHudIcon(GameEvent& event) noexcept
{
    if (!localPlayer)
        return;

    if (event.getInt("attacker") != localPlayer->getUserId())
        return;

    if (const auto iconOverride = iconOverrides[event.getString("weapon")])
        event.setString("weapon", iconOverride);
}

void SkinChanger::updateStatTrak(GameEvent& event) noexcept
{
    if (!localPlayer)
        return;

    if (const auto localUserId = localPlayer->getUserId(); event.getInt("attacker") != localUserId || event.getInt("userid") == localUserId)
        return;

    const auto weapon = localPlayer->getActiveWeapon();
    if (!weapon)
        return;

    if (const auto conf = get_by_definition_index(is_knife(weapon->itemDefinitionIndex2()) ? WEAPON_KNIFE : weapon->itemDefinitionIndex()); conf && conf->stat_trak > -1) {
        weapon->fallbackStatTrak() = ++conf->stat_trak;
        weapon->postDataUpdate(0);
    }
}

const std::vector<SkinChanger::PaintKit>& SkinChanger::getSkinKits() noexcept
{
    return skinKits;
}

const std::vector<SkinChanger::PaintKit>& SkinChanger::getGloveKits() noexcept
{
    return gloveKits;
}

const std::vector<SkinChanger::PaintKit>& SkinChanger::getStickerKits() noexcept
{
    return stickerKits;
}
